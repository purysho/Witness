# Authentication

Bearer access tokens expire after sixty minutes.

## Recovery

Expired sessions require a fresh login.

Recovery requires the user to authenticate again before a new token is issued.
